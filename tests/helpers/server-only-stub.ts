// Empty stub for `server-only`. Vitest aliases the package to this file
// so server-side modules can be imported in node-env tests without
// tripping the runtime guard. Production builds still see the real
// `server-only` package.
export {};
