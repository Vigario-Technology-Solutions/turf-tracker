# Dependencies
node_modules/

# Runtime data
data/
*.db
*.db-journal
*.db-shm
*.db-wal

# Environment
.env
.env.local
.env.*.local

# Sentry CLI / build plugin; contains a project-scoped auth token
# created on demand when running `sentry-cli login` or the build
# plugin's first source-map upload locally.
.env.sentry-build-plugin

# Next.js
.next/
out/

# Build artifacts
dist/
build/
bin/
# server.js at repo root is the build:server output. server.ts is the
# source; the .js is regenerated from it on every npm run build.
/server.js

# IDE
.vscode/
.idea/
.claude/settings.local.json
.claude/scheduled_tasks.lock

# OS
.DS_Store
Thumbs.db
nul

# Logs
*.log
npm-debug.log*

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Generated Prisma client
generated/

# Serwist generated service worker
public/sw*
public/swe-worker*
.eslintcache
prod/
temp/
